
{% comment %}
  Value Proposition Section
  Premium benefit cards showcasing core value
{% endcomment %}

<section 
  class="value-proposition section section--{{ section.settings.section_width }} color-{{ section.settings.color_scheme }}"
  style="{% render 'spacing-style', settings: section.settings %}"
>
  <div class="value-proposition__container page-width-content">
    {% if section.settings.heading != blank %}
      <div class="value-proposition__header animate-on-scroll">
        <h2 class="value-proposition__title">{{ section.settings.heading }}</h2>
      </div>
    {% endif %}

    <div class="value-proposition__grid">
      {% for block in section.blocks %}
        <div class="value-proposition__card animate-on-scroll" {{ block.shopify_attributes }} data-delay="{{ forloop.index | times: 50 }}">
          {% if block.settings.icon != blank %}
            <div class="value-proposition__icon">
              {% if block.settings.icon contains '.svg' %}
                <img 
                  src="{{ block.settings.icon | asset_url }}" 
                  alt="{{ block.settings.title | escape }}"
                  loading="lazy"
                >
              {% else %}
                <span class="value-proposition__icon-text">{{ block.settings.icon }}</span>
              {% endif %}
            </div>
          {% endif %}

          {% if block.settings.title != blank %}
            <h3 class="value-proposition__card-title">{{ block.settings.title }}</h3>
          {% endif %}

          {% if block.settings.description != blank %}
            <p class="value-proposition__card-description">{{ block.settings.description }}</p>
          {% endif %}
        </div>
      {% endfor %}
    </div>
  </div>
</section>

{% stylesheet %}
  .value-proposition {
    padding: 5rem 0;
  }

  @media (min-width: 750px) {
    .value-proposition {
      padding: 6rem 0;
    }
  }

  .value-proposition__container {
    width: 100%;
  }

  .value-proposition__header {
    text-align: center;
    margin-bottom: 3.5rem;
  }

  @media (min-width: 750px) {
    .value-proposition__header {
      margin-bottom: 4.5rem;
    }
  }

  .value-proposition__title {
    font-size: clamp(2rem, 4vw, 2.75rem);
    margin: 0;
    font-weight: 600;
    letter-spacing: -0.02em;
    line-height: 1.1;
  }

  .value-proposition__grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2rem;
  }

  @media (min-width: 750px) {
    .value-proposition__grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 2.5rem;
    }
  }

  @media (min-width: 990px) {
    .value-proposition__grid {
      grid-template-columns: repeat(4, 1fr);
      gap: 2rem;
    }
  }

  .value-proposition__card {
    text-align: center;
    padding: 2.5rem 1.5rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.25rem;
  }

  .value-proposition__icon {
    width: 56px;
    height: 56px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 0.5rem;
  }

  .value-proposition__icon img {
    width: 100%;
    height: 100%;
    object-fit: contain;
  }

  .value-proposition__icon-text {
    font-size: 2.5rem;
    line-height: 1;
  }

  .value-proposition__card-title {
    font-size: 1.25rem;
    font-weight: 600;
    margin: 0;
    letter-spacing: -0.01em;
    line-height: 1.3;
  }

  .value-proposition__card-description {
    font-size: 1rem;
    line-height: 1.5;
    opacity: 0.7;
    margin: 0;
    max-width: 280px;
  }

  /* Animations */
  .animate-on-scroll {
    opacity: 0;
    transform: translateY(20px);
    transition: opacity 0.6s ease-out, transform 0.6s ease-out;
  }

  .animate-on-scroll.animated {
    opacity: 1;
    transform: translateY(0);
  }

  @media (prefers-reduced-motion: reduce) {
    .animate-on-scroll {
      opacity: 1;
      transform: none;
      transition: none;
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Value Proposition",
  "tag": "section",
  "class": "section-wrapper",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Why choose us"
    },
    {
      "type": "select",
      "id": "section_width",
      "label": "Section Width",
      "options": [
        {
          "value": "page-width",
          "label": "Page Width"
        },
        {
          "value": "full-width",
          "label": "Full Width"
        }
      ],
      "default": "page-width"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color Scheme",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "Spacing"
    },
    {
      "type": "range",
      "id": "padding-block-start",
      "label": "Padding Top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "default": 80
    },
    {
      "type": "range",
      "id": "padding-block-end",
      "label": "Padding Bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "default": 80
    }
  ],
  "blocks": [
    {
      "type": "benefit",
      "name": "Benefit Card",
      "settings": [
        {
          "type": "text",
          "id": "icon",
          "label": "Icon",
          "info": "Emoji or upload SVG via theme files",
          "default": "✨"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Premium Quality"
        },
        {
          "type": "textarea",
          "id": "description",
          "label": "Description",
          "default": "Crafted with attention to detail for lasting value."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Value Proposition",
      "blocks": [
        {
          "type": "benefit",
          "settings": {
            "icon": "✨",
            "title": "Premium Quality",
            "description": "Thoughtfully designed with premium materials."
          }
        },
        {
          "type": "benefit",
          "settings": {
            "icon": "🚀",
            "title": "Fast Shipping",
            "description": "Delivered quickly and securely to your door."
          }
        },
        {
          "type": "benefit",
          "settings": {
            "icon": "💯",
            "title": "Satisfaction Guaranteed",
            "description": "We stand behind our products completely."
          }
        },
        {
          "type": "benefit",
          "settings": {
            "icon": "🔒",
            "title": "Secure Checkout",
            "description": "Your information is always protected."
          }
        }
      ]
    }
  ]
}
{% endschema %}

