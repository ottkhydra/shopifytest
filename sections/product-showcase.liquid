
{% comment %}
  Product Showcase Section
  Highlight product images with captions and features
{% endcomment %}

<section 
  class="product-showcase section section--{{ section.settings.section_width }} color-{{ section.settings.color_scheme }}"
  style="{% render 'spacing-style', settings: section.settings %}"
>
  <div class="product-showcase__container page-width-content">
    {% if section.settings.heading != blank %}
      <div class="product-showcase__header animate-on-scroll">
        <h2 class="product-showcase__title">{{ section.settings.heading }}</h2>
        {% if section.settings.subheading != blank %}
          <p class="product-showcase__subtitle">{{ section.settings.subheading }}</p>
        {% endif %}
      </div>
    {% endif %}

    <div class="product-showcase__grid">
      {% for block in section.blocks %}
        <div class="product-showcase__item animate-on-scroll" {{ block.shopify_attributes }} data-delay="{{ forloop.index | times: 100 }}">
          {% if block.settings.image != blank %}
            <div class="product-showcase__image-wrapper">
              {% assign showcase_img = block.settings.image | image_url: width: 1200 %}
              <img 
                src="{{ showcase_img }}" 
                loading="lazy"
                alt="{{ block.settings.caption | escape }}"
                class="product-showcase__image"
                sizes="(min-width: 990px) 50vw, 100vw"
              />
              {% if block.settings.feature_text != blank %}
                <div class="product-showcase__feature-badge">
                  {{ block.settings.feature_text }}
                </div>
              {% endif %}
            </div>
          {% endif %}

          {% if block.settings.caption != blank %}
            <div class="product-showcase__caption">
              <p>{{ block.settings.caption }}</p>
            </div>
          {% endif %}
        </div>
      {% endfor %}
    </div>
  </div>
</section>

{% stylesheet %}
  .product-showcase {
    padding: clamp(4.5rem, 6vw, 7rem) 0;
    background: #f9fafb;
  }

  .product-showcase__container {
    width: 100%;
  }

  .product-showcase__header {
    text-align: center;
    margin-bottom: clamp(3rem, 5vw, 4.5rem);
    max-width: 760px;
    margin-left: auto;
    margin-right: auto;
  }

  .product-showcase__title {
    font-size: clamp(2.1rem, 4vw, 2.9rem);
    margin: 0 0 0.75rem 0;
    font-weight: 600;
    letter-spacing: -0.02em;
    line-height: 1.15;
    color: #0f172a;
  }

  .product-showcase__subtitle {
    font-size: 1.05rem;
    line-height: 1.6;
    opacity: 0.78;
    margin: 0;
    color: #1f2937;
  }

  .product-showcase__grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: clamp(2rem, 4vw, 3.5rem);
  }

  @media (min-width: 900px) {
    .product-showcase__grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  .product-showcase__item {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
    background: #ffffff;
    border-radius: 16px;
    padding: clamp(1rem, 3vw, 1.5rem);
    box-shadow: 0 16px 40px rgba(15, 23, 42, 0.08);
    border: 1px solid rgba(15, 23, 42, 0.06);
  }

  .product-showcase__image-wrapper {
    position: relative;
    width: 100%;
    overflow: hidden;
    border-radius: 14px;
    aspect-ratio: 4 / 3;
    background: #f8fafc;
  }

  .product-showcase__image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.35s ease-out;
  }

  .product-showcase__item:hover .product-showcase__image {
    transform: scale(1.015);
  }

  .product-showcase__feature-badge {
    position: absolute;
    top: 1.25rem;
    left: 1.25rem;
    background: rgba(255, 255, 255, 0.9);
    backdrop-filter: blur(8px);
    padding: 0.55rem 1.1rem;
    border-radius: 999px;
    font-size: 0.9rem;
    font-weight: 600;
    color: #0f172a;
    border: 1px solid rgba(15, 23, 42, 0.08);
    box-shadow: 0 12px 30px rgba(15, 23, 42, 0.1);
  }

  .product-showcase__caption {
    font-size: 1.02rem;
    line-height: 1.6;
    color: #1f2937;
    opacity: 0.9;
    text-align: left;
    margin: 0;
  }

  .product-showcase__caption p {
    margin: 0;
  }

  .animate-on-scroll {
    opacity: 0;
    transform: translateY(16px);
    transition: opacity 0.5s ease-out, transform 0.5s ease-out;
  }

  .animate-on-scroll.animated {
    opacity: 1;
    transform: translateY(0);
  }

  @media (prefers-reduced-motion: reduce) {
    .animate-on-scroll {
      opacity: 1;
      transform: none;
      transition: none;
    }

    .product-showcase__image {
      transition: none;
    }

    .product-showcase__item:hover .product-showcase__image {
      transform: none;
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Product Showcase",
  "tag": "section",
  "class": "section-wrapper",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Designed for excellence"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading",
      "default": "Every detail matters"
    },
    {
      "type": "select",
      "id": "section_width",
      "label": "Section Width",
      "options": [
        {
          "value": "page-width",
          "label": "Page Width"
        },
        {
          "value": "full-width",
          "label": "Full Width"
        }
      ],
      "default": "page-width"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color Scheme",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "Spacing"
    },
    {
      "type": "range",
      "id": "padding-block-start",
      "label": "Padding Top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "default": 96
    },
    {
      "type": "range",
      "id": "padding-block-end",
      "label": "Padding Bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "default": 96
    }
  ],
  "blocks": [
    {
      "type": "showcase",
      "name": "Showcase Item",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "feature_text",
          "label": "Feature Badge Text",
          "info": "Optional feature highlight badge"
        },
        {
          "type": "text",
          "id": "caption",
          "label": "Caption",
          "default": "Premium craftsmanship in every detail"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Product Showcase",
      "blocks": [
        {
          "type": "showcase",
          "settings": {
            "feature_text": "Premium Materials",
            "caption": "Crafted with attention to detail"
          }
        },
        {
          "type": "showcase",
          "settings": {
            "feature_text": "Thoughtful Design",
            "caption": "Every element serves a purpose"
          }
        }
      ]
    }
  ]
}
{% endschema %}

